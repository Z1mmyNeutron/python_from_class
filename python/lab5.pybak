file = pickAFile()
picture = makePicture(file)

def rectangleSelect(picture, (x0, y0), (x1,y1)):
  for i in range():
    for j in range():

def copyAndPasteAsNewImage(picture, x, y):
  
  return copyAndPasteAsNewImage